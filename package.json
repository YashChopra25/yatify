{
  "name": "yatify",
  "version": "1.0.0",
  "description": "> # Step 1: Getting Source Code\r >Clone the repo [Yatify](https://github.com/YashChopra25/yatify),then follow the next steps\r >\r > #  Step 2: Add the Environment variable to the App\r >\r > * Go to the root folder\r >    * create an environment file using this command `cp sample.env .env` to change the environment values according to the requirements\r > \r > * Go to the client folder using this command `cd client`\r >    * create an environment file using this command `cp sample.env .env` to change the environment values according to the requirements\r >\r >* **Open a new terminal in your IDE/ Use the command to go back to the terminal `cd ..`**\r > * Go to the server folder using this command `cd server`\r >    * create an environment file using this command `cp sample.env .env` to change the environment values according to the requirements\r >\r ># Step 3:\r >\r >## Method 1: With Docker\r > * Go to the root directory\r >\r >  *Note:**Docker** must be installed in your system and it should be in the running state*\r >## Download [Docker](https://docs.docker.com/desktop/setup/install/windows-install/)\r >For verifying whether the docker is running or not use this command in your terminal `docker ps`\r >\r >* Open the terminal in the root directory and run this command `docker compose up --build` use this flag for use in the detached mode `-d` with the command\r >TO close the container  run this command `docker compose down`\r >\r >\r >The above command file read the Docker file located in the server and client folder and then pull the image for the first time if exist else it will used the previous image,then run the server on that port.\r >\r >***Note**: This can take a bit longer as it depends on the network speed*\r > ## Method 2: Without Docker\r > * Go to the client folder using this command `cd client`\r > \r >    * install the dependency using this command `npm i`\r >    * Run  the server using this command `npm run dev`\r >* **Open an new terminal in your IDE**\r > * Go to the server folder using this command `cd server`\r >    * install the dependency using this command `npm i`\r >    * Run  the server using this command `npm run dev`\r >\r >>Open the browser and open this URL: [Link](http://localhost:5173)\r > The backend is running on the PORT 8000\r >Open the browser and open this URL: [Link](http://localhost:5173)\r >\r > The backend is running on the PORT 8000\r >",
  "scripts": {
    "start": "docker compose up --build",
    "stop": "docker compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
